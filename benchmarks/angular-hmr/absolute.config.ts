import { defineConfig } from '@absolutejs/absolute';

export default defineConfig({
	angularDirectory: 'angular',
	assetsDirectory: 'assets',
	buildDirectory: 'build',
	publicDirectory: 'public',
	stylesConfig: 'styles/indexes'
});
