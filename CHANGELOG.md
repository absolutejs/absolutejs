# Changelog

All notable changes to `@absolutejs/absolute`.

This file is generated by `absolute-changelog` from the entries in
`changelog/`. Edit an entry, not this file — and add new ones under
`changelog/unreleased/`.

## 0.20.0-beta.113 — 2026-09-17

### Added

- **Mobile CI promotions survive local interruption without duplicate dispatch** (`ABSOLUTE_MOBILE_CI_PROMOTION_OPERATION_FORMAT`, `AbsoluteMobilePromotionOperation`, `AbsoluteMobilePromotionOperationPhase`, `AdvanceAbsoluteMobilePromotionOperationOptions`, `CreateAbsoluteMobilePromotionOperationOptions`, `absoluteMobilePromotionOperationPath`, `advanceAbsoluteMobilePromotionOperation`, `createAbsoluteMobilePromotionOperation`, `listAbsoluteMobilePromotionOperations`, `parseAbsoluteMobilePromotionOperation`, `readAbsoluteMobilePromotionOperation`, `relativeAbsoluteMobilePromotionOperationPath`)
  AbsoluteJS fsyncs and atomically installs a bounded non-secret promotion operation before contacting GitHub, advances the same identity through exact run discovery, completion and audit, lists local operations, and resumes persisted intent after sleep, disconnect or process failure without issuing another workflow dispatch.

## 0.20.0-beta.112 — 2026-09-17

### Added

- **Mobile CI promotions correlate, watch, and audit the exact dispatched workflow run** (`ABSOLUTE_MOBILE_CI_PROMOTION_CONTEXT_FORMAT`, `ABSOLUTE_MOBILE_CI_WORKFLOW_FORMAT`, `AbsoluteMobileCiRunStatus`, `AbsoluteMobilePromotionAudit`, `AbsoluteMobilePromotionContext`, `AbsoluteMobilePromotionRun`, `discoverAbsoluteMobilePromotionRun`, `parseAbsoluteMobilePromotionRunList`)
  The promotion CLI assigns each protected dispatch a unique non-secret identity, discovers only the GitHub Actions run carrying that identity, returns its run ID and URL, optionally waits for completion, and can chain an independent local audit without racing concurrent promotions; retained format-1 audit context remains readable.

## 0.20.0-beta.111 — 2026-09-17

### Added

- **Mobile CI gains first-class promotion status and independent artifact auditing** (`ABSOLUTE_MOBILE_CI_PROMOTION_AUDIT_FORMAT`, `ABSOLUTE_MOBILE_CI_PROMOTION_CONTEXT_FORMAT`, `ABSOLUTE_MOBILE_CI_WORKFLOW_FORMAT`, `AbsoluteMobileCiCommandResult`, `AbsoluteMobileCiCommandRunner`, `AbsoluteMobileCiPlatform`, `AbsoluteMobileCiRunStatus`, `AbsoluteMobilePromotionAudit`, `AbsoluteMobilePromotionContext`, `auditAbsoluteMobileCiPromotion`, `inspectAbsoluteMobileCiRun`, `parseAbsoluteMobileCiRunStatus`, `parseAbsoluteMobilePromotionContext`, `watchAbsoluteMobileCiRun`)
  `absolute mobile ci status` reports or watches a bounded GitHub run identity, while `absolute mobile ci audit` downloads the exact retained AAB or IPA and promotion evidence, re-hashes the release, re-verifies installed-app certification and Sigstore provenance, validates the publication receipt, and emits sanitized reproducible reports without credentials.

## 0.20.0-beta.110 — 2026-09-17

### Added

- **Mobile CI resumes and promotes exact certified artifacts with portable Sigstore provenance** (`ABSOLUTE_MOBILE_CI_WORKFLOW_FORMAT`, `ABSOLUTE_MOBILE_CERTIFICATION_VERIFICATION_FORMAT`, `AbsoluteMobileCertificationVerification`, `AbsoluteNativeReleasePublication`, `AbsoluteNativeReleasePublisher`, `PublishAbsoluteAndroidReleaseOptions`, `createAbsoluteMobileCertificationVerification`, `parseAbsoluteMobileCertificationVerification`, `publishAbsoluteIosRelease`, `readAbsoluteMobileCertificationVerification`, `readAbsoluteSigstoreBundle`, `serializeAbsoluteMobileReleaseCertification`, `writeAbsoluteMobileCertificationVerification`)
  A protected follow-up GitHub workflow restores a selected prior-run AAB or IPA without rebuilding, re-hashes it, verifies imported installed-app certification, signs the exact certification with GitHub OIDC and pinned Cosign, requires trusted registry provenance, publishes the existing artifact, and retains the final audit receipt.

## 0.20.0-beta.109 — 2026-09-17

### Added

- **Mobile publication promotes the exact certified AAB or IPA with secure production policies and generated acceptance CI** (`AbsoluteMobileCertifiableRelease`, `AbsoluteNativeReleasePublication`, `AbsoluteNativeReleasePublisher`, `NormalizedAbsoluteMobileConfig`, `PublishAbsoluteAndroidReleaseOptions`, `publishAbsoluteIosRelease`)

## 0.20.0-beta.108 — 2026-09-16

### Fixed

- **Keep package-owned Vue components and helpers inside separate browser and server output trees, preserving relative imports, cache restoration and hot-reload identities.**

## 0.20.0-beta.107 — 2026-09-16

### Added

- **Mobile releases gain content-addressed installed-app certification** (`ABSOLUTE_MOBILE_RELEASE_CERTIFICATION_FORMAT`, `AbsoluteMobileCertifiableRelease`, `AbsoluteMobileCertificationEvidence`, `AbsoluteMobileCertificationRelease`, `AbsoluteMobileCertificationRequirement`, `AbsoluteMobileCertificationStrength`, `AbsoluteMobileReleaseCertification`, `AbsoluteMobileReleaseCertificationPaths`, `CreateAbsoluteMobileReleaseCertificationOptions`, `createAbsoluteMobileReleaseCertification`, `readAbsoluteMobileReleaseCertification`, `renderAbsoluteMobileReleaseCertification`, `verifyAbsoluteMobileReleaseCertification`, `writeAbsoluteMobileReleaseCertification`)
  `absolute mobile certify` revalidates immutable Android and iOS releases plus sanitized installed-app reports, binds the exact artifact, embedded runtime, app version, and evidence tier into an atomic content-addressed record, and verifies promotion policy without native tools or credentials. Android requires installed offline proof; iOS preserves the honest Simulator, registered-device, and Apple-processed TestFlight hierarchy.

## 0.20.0-beta.106 — 2026-09-16

### Added

- **Remote Macs run all three installed iOS release acceptance lanes** (`ABSOLUTE_REMOTE_MAC_PROTOCOL_VERSION`, `AbsoluteRemoteIosReleaseAcceptance`, `BuildAbsoluteRemoteIosReleaseOptions`, `RunAbsoluteRemoteIosReleaseAcceptanceOptions`, `cleanAbsoluteRemoteMacWorkspace`, `inspectAbsoluteRemoteMacWorkspace`, `runAbsoluteIosDeviceReleaseAcceptance`, `runAbsoluteIosSimulatorReleaseAcceptance`, `runAbsoluteRemoteIosReleaseAcceptance`, `syncAbsoluteRemoteMacAcceptanceRelease`)
  Paired developer-owned Macs can now export and return the same-archive registered-device IPA, then execute source-equivalent Simulator, archive-equivalent registered-device, and store-delivered TestFlight acceptance from Windows or Linux. Protocol v4 atomically synchronizes and re-hashes immutable releases, validates returned evidence locally, propagates cancellation into Xcode and device tools, and emits sanitized remote-host reports and phase telemetry without transferring signing or publication credentials.

## 0.20.0-beta.105 — 2026-09-16

### Added

- **iOS releases gain three-lane installed-app acceptance** (`AbsoluteIosReleaseMetadata`, `BuildAbsoluteIosReleaseOptions`, `buildAbsoluteIosRelease`, `buildAbsoluteRemoteIosRelease`, `installAbsoluteIosRelease`, `readAbsoluteIosRelease`, `requireAbsoluteIosReleaseMetadata`, `runAbsoluteIosDeviceReleaseAcceptance`, `runAbsoluteIosSimulatorReleaseAcceptance`)
  `absolute mobile test ios --release` now records honest source-equivalent Simulator, same-archive registered-device, or Apple-processed TestFlight evidence. Capacitor and Expo emit version/build-specific native readiness markers, immutable releases can include a separately hashed registered-device IPA, physical offline claims require explicit confirmation, and sanitized reports retain only artifact classification, outcome, and bounded timings.

## 0.20.0-beta.104 — 2026-09-16

### Added

- **Capacitor Android releases gain installed offline AAB acceptance**
  The provider-neutral release command now installs the exact Capacitor AAB-derived APK set on the managed emulator, takes both network transports offline, and requires an AbsoluteJS-owned native readiness signal across a cold launch and process relaunch. Bun workspace-hoisted Capacitor executables and package manifests are resolved only through the application’s governing workspace. Reports and telemetry retain only provider, artifact identity, outcome, and bounded timings.

## 0.20.0-beta.103 — 2026-09-16

### Added

- **Expo Android releases now have installed offline AAB acceptance**
  `absolute mobile test android --release` revalidates one immutable AAB, approval-installs checksum-pinned Bundletool, starts the managed emulator, installs the derived APK set, and proves embedded content across two offline process launches. Expo production web files travel through Metro as one deterministic opaque archive and are reconstructed from a verified offset table, avoiding per-file registry failures. Sanitized reports and telemetry record only artifact identity, sizes, timings, provider, and outcome.

## 0.20.0-beta.101 — 2026-09-16

### Fixed

- **WSL Expo Android releases now pass public-CLI conformance** (`buildAbsoluteAndroidRelease`)
  The exact public build command now resolves React vendor assets from linked packages, recognizes declared workspace lockfiles without trusting unrelated ancestors, avoids false-positive production HMR detection, prints per-phase timings, and verifies AAB signatures with translated paths through the Windows JDK. A permanent two-build gate validates signing, immutable metadata, all production ABIs, bundled JavaScript, release hygiene, and warm Gradle reuse.

## 0.20.0-beta.100 — 2026-09-15

### Added

- **Expo Android production builds now run from WSL** (`BuildAbsoluteExpoAndroidWslBundleOptions`, `buildAbsoluteExpoAndroidWslBundle`, `buildAbsoluteAndroidRelease`)
  AbsoluteJS projects generated in WSL build through a serialized Windows-local mirror with Windows Bun, a short temporary drive, full release ABIs, retained warm caches, sanitized phase telemetry, and the existing signing, doctor, immutable-artifact, and publishing contracts.

## 0.20.0-beta.99 — 2026-09-15

### Fixed

- **Expo development recovers from Metro startup port races** (`AbsoluteExpoDevPhaseTiming`, `AbsoluteExpoDevSession`, `StartAbsoluteExpoDevOptions`, `startAbsoluteExpoDevSession`)
  AbsoluteJS retries a newly allocated loopback port without prompting, propagates the final port through local, WSL, and Remote Mac native targets, preserves it across rebuilds, and emits only a categorical reason plus bounded retry count.

## 0.20.0-beta.98 — 2026-09-15

### Added

- **Expo Android development now has a performance and accessibility quality gate** (`DEFAULT_ABSOLUTE_EXPO_ANDROID_QUALITY_BUDGETS`, `parseAbsoluteAndroidAccessibilityHierarchy`, `parseAbsoluteExpoAndroidLaunchTiming`, `parseAbsoluteExpoAndroidMemory`)
  An installed API 36 gate measures cold and warm launch, bridge and all-framework HMR p95, memory growth, native rebuild/reconnect, and native plus embedded-web accessibility while emitting only sanitized timing artifacts.

## 0.20.0-beta.97 — 2026-09-15

### Fixed

- **Expo Android picker recovery distinguishes explicit cancellation from delayed successful results**
  Generated Expo shells forward a trusted, request-correlated native cancellation signal to the provider-neutral device lifecycle, while the installed API 36 gate covers camera and multi-photo success, cancellation, stale-result suppression, exactly-once delivery, and native-path redaction.

## 0.20.0-beta.96 — 2026-09-15

### Added

- **Expo Android restores in-flight camera results after background process death** (`ABSOLUTE_EXPO_BRIDGE_METHODS`, `parseAbsoluteExpoBridgeMessage`)
  Generated Expo shells expose the provider-neutral restored-operation queue through the allowlisted bridge, and an installed API 36 gate proves exactly-once camera recovery without leaking native file paths.

### Fixed

- **WSL Expo Android rebuilds preserve a compatible Windows Bun lockfile until source dependencies change**
  AbsoluteJS hashes the source lock input and invalidates the mirrored Windows lock only when dependencies change, avoiding repeated resolution while keeping each host Bun version in control of its own lock format.

## 0.20.0-beta.93 — 2026-09-13

### Added

- **Expo development now preserves one Metro process across automatic native rebuilds and provides installed-app HMR conformance for native React plus every supported embedded framework.** (`AbsoluteExpoDevSession`, `AbsoluteExpoNativeChange`, `AbsoluteExpoNativeWatcher`, `AbsoluteExpoNativeWatcherOptions`, `createAbsoluteExpoNativeWatcher`, `fingerprintAbsoluteExpoNativeInputs`, `isAbsoluteExpoNativeRootInput`, `startAbsoluteExpoDevSession`, `startAbsoluteRemoteExpoIosDevSession`, `writeAbsoluteExpoProject`)
  Native inputs are fingerprinted and coalesced, local and Remote Mac sessions share a serialized rebuild contract, generated WebView hosts can select an AbsoluteJS path, native timing logs and telemetry remain data-minimal, and Android conformance verifies state preservation, rebuild, and reconnect.

### Fixed

- **Svelte rune state now remains deterministic across template-only HMR updates.**
  AbsoluteJS now lets Svelte's compiler-owned HMR wrapper complete its state restoration instead of racing it with a redundant remount.

## 0.20.0-beta.92 — 2026-09-13

### Changed

- **Expo iOS now has an automated native observability gate for generated Swift compilation, trusted-relay redaction, restart-safe retry, acknowledgement, and deduplication.** (`expoNativeObservabilityRuntimeSource`)

## 0.20.0-beta.91 — 2026-09-13

### Added

- **Expo iOS now has an automated installed-app replacement gate for native Auth, encrypted Sync migration, and exactly-once recovery.** (`AbsoluteIosInstalledApp`, `AbsoluteIosUpgradeCommandResult`, `assertAbsoluteIosInstalledAppUpgrade`, `inspectAbsoluteIosInstalledApp`, `parseAbsoluteIosInstalledAppInfo`)

## 0.20.0-beta.90 — 2026-09-13

### Added

- **Installed Expo iOS apps now have a production Simulator conformance gate for signed OTA safety, native Auth and Sync continuity, staged rollout controls, fleet recovery, and restart-safe reporting.**

## 0.20.0-beta.89 — 2026-09-13

### Added

- **Installed Expo Android apps now exercise staged cohorts, automatic advancement, pause, resume, cancellation, fleet recovery, and restart-safe health reporting in the native conformance suite.**
